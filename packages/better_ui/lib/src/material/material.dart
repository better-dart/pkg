import 'index.dart';

///
/// 自定义组件列表:
///
class BetterMaterial {
  // 按钮:
  BetterButton button = BetterButton();

  // 文本组件:
  BetterTextField field = BetterTextField();

  BetterListTile listTile = BetterListTile();
}
